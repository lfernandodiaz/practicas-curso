mutation newUserMutation($email: String!, $firstname: String!, $lastname: String!, $validate: Boolean!) {
  newUser(email: $email, firstname: $firstname, lastname: $lastname, validate: $validate){
    Id
    Href
    DocumentId
  }
}
